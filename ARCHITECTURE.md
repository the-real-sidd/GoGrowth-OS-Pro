# 🏗️ MERN Stack Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     CLIENT APPLICATION (React)                   │
│                        Port 3000                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │  Dashboard   │  │  Resources   │  │    Login     │           │
│  │    Page      │  │    Page      │  │    Page      │           │
│  └──────────────┘  └──────────────┘  └──────────────┘           │
│         ▲                ▲                  ▲                    │
│         └────────────────┴──────────────────┘                    │
│                         │                                         │
│  ┌──────────────────────▼──────────────────────┐                 │
│  │           App.js (Main Component)           │                 │
│  └────────────────────────────────────────────┘                 │
│                         │                                         │
│  ┌──────────────────────▼──────────────────────┐                 │
│  │    Custom Hooks (State Management)          │                 │
│  │  useTasks  │  useResources  │  useAuth     │                 │
│  └────────────────────────────────────────────┘                 │
│                         │                                         │
│  ┌──────────────────────▼──────────────────────┐                 │
│  │         API Service (services/api.js)       │                 │
│  │              Axios Client                   │                 │
│  └────────────────────────────────────────────┘                 │
└─────────────────────────────────────────────────────────────────┘
                          │ HTTP Requests/Responses
                          │ (REST API Calls)
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                 SERVER APPLICATION (Express)                     │
│                        Port 5000                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐                 │
│  │   Task     │  │  Resource  │  │   User     │                 │
│  │   Routes   │  │   Routes   │  │   Routes   │                 │
│  └─────▲──────┘  └─────▲──────┘  └─────▲──────┘                 │
│        │                │                │                       │
│  ┌─────┴────────────────┴────────────────┴──────┐                │
│  │      Express Server (server.js)              │                │
│  │  CORS Enabled, JSON Parser, Error Handler   │                │
│  └─────────────────────────────────────────────┘                │
│        ▲                ▲                ▲                       │
│        │                │                │                       │
│  ┌─────┴──────┐  ┌─────┴──────┐  ┌─────┴──────┐                │
│  │   Task     │  │  Resource  │  │   User     │                │
│  │ Controller │  │ Controller │  │ Controller │                │
│  └─────▲──────┘  └─────▲──────┘  └─────▲──────┘                │
│        │                │                │                       │
│  ┌─────┴────────────────┴────────────────┴──────┐                │
│  │  Middleware (JWT Authentication)            │                │
│  └─────────────────────────────────────────────┘                │
│        ▲                                                         │
│        │                                                         │
└─────────────────────────────────────────────────────────────────┘
          │ Query/Response
          │ Authentication
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    DATABASE LAYER (MongoDB)                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │     Task     │  │   Resource   │  │     User     │           │
│  │   Collection │  │  Collection  │  │  Collection  │           │
│  │              │  │              │  │              │           │
│  │ - id         │  │ - id         │  │ - id         │           │
│  │ - task       │  │ - title      │  │ - name       │           │
│  │ - assignedTo │  │ - category   │  │ - email      │           │
│  │ - status     │  │ - description│  │ - password   │           │
│  │ - client     │  │ - url        │  │ - role       │           │
│  │ - deadline   │  │ - type       │  │ - picture    │           │
│  │ - etc        │  │ - status     │  │ - isActive   │           │
│  └──────────────┘  └──────────────┘  └──────────────┘           │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow Diagram

```
1. USER INTERACTION (Frontend)
   ┌─────────────────────────────────────────┐
   │ User clicks "Add Task" button            │
   └──────────────┬──────────────────────────┘
                  │
2. COMPONENT STATE (React)
   ┌──────────────▼──────────────────────────┐
   │ TaskModal opens, form state initialized │
   └──────────────┬──────────────────────────┘
                  │
3. HOOK STATE (Custom Hook)
   ┌──────────────▼──────────────────────────┐
   │ useTasks hook receives form data        │
   │ Calls: createTask(taskData)             │
   └──────────────┬──────────────────────────┘
                  │
4. API CALL (Service)
   ┌──────────────▼──────────────────────────┐
   │ api.js makes HTTP POST request          │
   │ POST http://localhost:5000/api/tasks   │
   │ Headers: { Authorization: "Bearer ..."}
   └──────────────┬──────────────────────────┘
                  │
                  ▼ (Network Request)
                  
5. SERVER RECEIVES REQUEST (Express)
   ┌──────────────────────────────────────────┐
   │ Express route handler receives request   │
   │ Path: POST /api/tasks                    │
   └──────────────┬──────────────────────────┘
                  │
6. MIDDLEWARE (Authentication)
   ┌──────────────▼──────────────────────────┐
   │ JWT auth middleware validates token     │
   │ req.userId is set                       │
   └──────────────┬──────────────────────────┘
                  │
7. CONTROLLER (Business Logic)
   ┌──────────────▼──────────────────────────┐
   │ taskController.createTask() processes   │
   │ Validates data                          │
   │ Creates Task object                     │
   └──────────────┬──────────────────────────┘
                  │
8. DATABASE SAVE (MongoDB)
   ┌──────────────▼──────────────────────────┐
   │ Mongoose saves to MongoDB               │
   │ INSERT into Task collection             │
   │ Returns: { _id, task, status, ... }    │
   └──────────────┬──────────────────────────┘
                  │
                  ▼ (Network Response)
                  
9. RESPONSE SENT (Express)
   ┌──────────────────────────────────────────┐
   │ HTTP 201 Created                         │
   │ JSON: { _id, task, status, ... }       │
   └──────────────┬──────────────────────────┘
                  │
10. API RESPONSE (Service)
    ┌──────────────▼──────────────────────────┐
    │ API service receives response           │
    │ Returns data to React hook              │
    └──────────────┬──────────────────────────┘
                   │
11. STATE UPDATE (Custom Hook)
    ┌──────────────▼──────────────────────────┐
    │ useTasks updates state                  │
    │ setTasks([...tasks, newTask])          │
    └──────────────┬──────────────────────────┘
                   │
12. RE-RENDER (React)
    ┌──────────────▼──────────────────────────┐
    │ Components re-render with new task      │
    │ Modal closes                            │
    │ New task appears in list                │
    └──────────────────────────────────────────┘
```

## Component Hierarchy

```
App.js (Root)
│
├─ Header
│  ├─ Logo
│  ├─ Navigation (Dashboard, Resources)
│  ├─ Theme Toggle
│  ├─ Add Task Button
│  └─ User Profile
│
├─ Main Content
│  │
│  ├─ Dashboard Page
│  │  ├─ Statistics Component
│  │  │  ├─ Stats Cards (Total, Completed, In Progress, Pending)
│  │  │  └─ Date Filter
│  │  │
│  │  ├─ TaskFilters Component
│  │  │  ├─ Search Input
│  │  │  ├─ Filter by Status
│  │  │  ├─ Filter by Assigned To
│  │  │  └─ Filter by Client
│  │  │
│  │  └─ TaskList
│  │     └─ TaskCard (repeating)
│  │        ├─ Task Title
│  │        ├─ Status Badge
│  │        ├─ Priority Badge
│  │        ├─ Task Info
│  │        ├─ Edit Button
│  │        └─ Delete Button
│  │
│  ├─ TaskModal (Overlay)
│  │  ├─ Form Title
│  │  ├─ Task Name Input
│  │  ├─ Assigned To Input
│  │  ├─ Client Input
│  │  ├─ Status Select
│  │  ├─ Priority Select
│  │  ├─ Deadline Date
│  │  ├─ Remarks Textarea
│  │  ├─ Cancel Button
│  │  └─ Save Button
│  │
│  ├─ Resources Page
│  │  ├─ Page Header with Add Button
│  │  ├─ Resource Filters
│  │  └─ ResourceCard (repeating)
│  │     ├─ Title
│  │     ├─ Category
│  │     ├─ Description
│  │     ├─ Type
│  │     ├─ Visit Button
│  │     ├─ Edit Button
│  │     └─ Delete Button
│  │
│  └─ Login Page (if not authenticated)
│     ├─ Logo
│     ├─ Email Input
│     ├─ Password Input
│     ├─ Login Button
│     └─ Demo Credentials Note
│
├─ Notification (Toast)
│  ├─ Message
│  ├─ Type (success, error, info)
│  └─ Close Button
│
└─ Loader
   └─ Spinner Animation
```

## API Architecture

```
BASE URL: http://localhost:5000/api

TASK ENDPOINTS
├─ GET    /tasks              ← Get all tasks
├─ POST   /tasks              ← Create task (requires auth)
├─ PUT    /tasks/:id          ← Update task (requires auth)
├─ DELETE /tasks/:id          ← Delete task (requires auth)
└─ GET    /tasks/stats        ← Get statistics

RESOURCE ENDPOINTS
├─ GET    /resources          ← Get all resources
├─ POST   /resources          ← Create resource (requires auth)
├─ PUT    /resources/:id      ← Update resource (requires auth)
└─ DELETE /resources/:id      ← Delete resource (requires auth)

USER ENDPOINTS
├─ POST   /users/login        ← Login user
├─ POST   /users/register     ← Register new user
├─ GET    /users/profile      ← Get current user (requires auth)
└─ GET    /users              ← Get all users (requires auth)
```

## State Management Flow

```
REACT COMPONENT
        │
        ▼
    USES HOOK
        │
        ▼
   useTasks() Hook
   useSources() Hook
   useAuth() Hook
        │
        ▼
   HOOK STATE (useState)
        │
        ▼
   API SERVICE CALL
   (Axios)
        │
        ▼
   EXPRESS BACKEND
   Server.js → Routes → Controllers
        │
        ▼
   MongoDB Database
        │
        ▼
   DATA RETURNED
        │
        ▼
   HOOK UPDATES STATE
   (setState)
        │
        ▼
   COMPONENT RE-RENDERS
```

## Technology Stack Layers

```
┌──────────────────────────────────────────────────┐
│   PRESENTATION LAYER (React 18)                  │
│   - Components                                   │
│   - Hooks                                        │
│   - CSS Styling                                  │
└─────────────────────┬──────────────────────────┘
                      │
┌─────────────────────▼──────────────────────────┐
│   API/SERVICE LAYER                             │
│   - Axios API Client                            │
│   - Custom Hooks for State                      │
│   - HTTP Communication                          │
└─────────────────────┬──────────────────────────┘
                      │
┌─────────────────────▼──────────────────────────┐
│   SERVER LAYER (Express.js)                     │
│   - Routes                                      │
│   - Controllers                                 │
│   - Middleware (Auth, CORS)                    │
│   - Business Logic                              │
└─────────────────────┬──────────────────────────┘
                      │
┌─────────────────────▼──────────────────────────┐
│   DATABASE LAYER (MongoDB)                      │
│   - Mongoose Models                             │
│   - Data Collections                            │
│   - Data Persistence                            │
└──────────────────────────────────────────────┘
```

## File Responsibilities

```
Frontend (client/src/)
│
├─ pages/             ← Full page components
│  ├─ Dashboard.js    ← All dashboard logic
│  ├─ Resources.js    ← All resources logic
│  └─ Login.js        ← Authentication UI
│
├─ components/        ← Reusable components
│  ├─ Header.js       ← Navigation
│  ├─ TaskCard.js     ← Single task display
│  ├─ TaskModal.js    ← Form modal
│  ├─ TaskFilters.js  ← Filter panel
│  ├─ Statistics.js   ← Stats display
│  ├─ Loader.js       ← Loading spinner
│  └─ Notification.js ← Toast messages
│
├─ hooks/             ← State management
│  ├─ useTasks.js     ← Task state & logic
│  ├─ useResources.js ← Resource state
│  └─ useAuth.js      ← Auth state
│
├─ services/          ← API communication
│  └─ api.js          ← Axios setup & calls
│
└─ styles/            ← Organized CSS
   ├─ header.css
   ├─ taskCard.css
   ├─ filters.css
   └─ ... (one per component)

Backend (server/)
│
├─ models/            ← Data schemas
│  ├─ Task.js
│  ├─ Resource.js
│  └─ User.js
│
├─ controllers/       ← Business logic
│  ├─ taskController.js
│  ├─ resourceController.js
│  └─ userController.js
│
├─ routes/            ← URL endpoints  
│  ├─ taskRoutes.js
│  ├─ resourceRoutes.js
│  └─ userRoutes.js
│
├─ middleware/        ← Custom middleware
│  └─ auth.js         ← JWT authentication
│
├─ server.js          ← Express server
└─ config/            ← Configuration
   └─ database.js     ← MongoDB connection
```

---

This architecture provides:
- ✅ Clear separation of concerns
- ✅ Scalability for future features
- ✅ Easy testing and debugging
- ✅ Professional code organization
- ✅ Proper error handling
- ✅ Security with authentication

**Study this diagram to understand your MERN application!** 📚
